# This project currently does not require runtime secrets in source control.
# Use this file as a template if future environments need private values.
#
# Example:
# API_BASE_URL=https://api.example.com
# SENTRY_DSN=
